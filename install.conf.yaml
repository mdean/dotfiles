- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.gvimrc: gvimrc
    ~/.vim: vim
    ~/.vimrc: vimrc

- shell:
    - [git submodule update --init --recursive, Installing submodules]
