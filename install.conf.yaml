- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.antigenrc: antigenrc
    ~/.gvimrc: gvimrc
    ~/.vim: vim
    ~/.vimrc: vimrc
    ~/.zshrc: zshrc

- shell:
    - [git submodule update --init --recursive, Installing submodules]
